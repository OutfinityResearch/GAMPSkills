# copy-file

## Description
Copies a single file from a source path to a destination path. Ensures the destination directory exists and overwrites the target if it exists.

## Instructions
Accept source and destination paths as strings. Check that the source file exists and is readable. Create the destination directory if necessary. Perform the file copy operation, handling errors such as permissions or invalid paths.