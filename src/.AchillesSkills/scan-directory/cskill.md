# scan-directory

## Description
Lists files in a directory with optional filtering by patterns, recursion, and exclusions. Returns an array of relative file paths matching the criteria.

## Instructions
Accept a root directory path and options for recursion, include/exclude patterns. Traverse the directory structure as specified, applying filters to collect matching file paths. Return the array of relative paths, handling access errors.